{
  "name": "firstsubgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ genesis3800/firstsubgraph",
    "create-local": "graph create --node http://localhost:8020/ genesis3800/firstsubgraph",
    "remove-local": "graph remove --node http://localhost:8020/ genesis3800/firstsubgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 genesis3800/firstsubgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
